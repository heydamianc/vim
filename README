Set Up
======

To use:
1. git clone <remote git project location>.git vim
2. ln -s /<full path>/vim/.vimrc ~/.vimrc
3. ln -s /<full path>/vim/.vim ~/.vim
4. mkdir ~/.vim_backup

The vim set up should be generic for all platforms. Tested with MacVim.

License
=======

This project is released under the MIT license.

